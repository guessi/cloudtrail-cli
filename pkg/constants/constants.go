package constants

const (
	NAME  string = "cloudtrail-cli"
	USAGE string = "Blazing fast single purpose cli for CloudTrail log filtering"
)

var (
	GitVersion string
	GoVersion  string
	BuildTime  string
)
